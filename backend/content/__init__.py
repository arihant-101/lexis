"""Practice-item domain: item models, error taxonomy, and ingestion.

Note: error taxonomy lives here (content/taxonomy.py) rather than the planned
eval/ path to avoid colliding with the existing evals/ test-suite package.
"""
